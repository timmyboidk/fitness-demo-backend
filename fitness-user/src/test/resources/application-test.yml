spring:
  datasource:
    url: jdbc:tc:mysql:8.0:///fitness_db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    compatibility-verifier:
      enabled: false

app:
  jwt:
    secret: test-secret-key-1234567890-very-long-and-secure-key-for-hmac-sha256
    expiration: 3600
  encrypt:
    key: test-encrypt-key
